return {
	"goolord/alpha-nvim",
	enabled = false,
	lazy = false,
	opts = {
		layout = {
			{ type = "padding", val = 2 },
			{
				type = "text",
				val = {
					"Preconfigured",
					"Neovim v" .. vim.version().major .. "." .. vim.version().minor .. "." .. vim.version().patch,
				},
				opts = {
					hl = "Constant",
				},
			},
		},
		opts = {
			margin = 2,
		},
	},
}
